# LiteRGSS3

## Before compilation

1. Download Ruby with Devkit (version 3.4.x, 32 bits)
2. Download Raylib (for Windows: raylib-5.5_win32_mingw-w64)
3. Extract raylib in vendor folder

## Compilation

1. `ridk enable`
2. `rake compile` (to clean: `rake clean`)
3. To test, `ruby ./start.rb`
